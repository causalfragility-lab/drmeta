% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dr_plot.R
\name{dr_plot}
\alias{dr_plot}
\title{Weight Diagnostic Plot for DR-Meta}
\usage{
dr_plot(
  object,
  col_pts = "#2166AC",
  col_curve = "#D6604D",
  xlab = expression(paste("Design robustness (", DR[i], ")")),
  ylab = "DR-Meta weight (normalised, \%)",
  main = "DR-Meta Weight vs Design Robustness",
  show_labels = TRUE,
  ...
)
}
\arguments{
\item{object}{A fitted \code{"drmeta"} object.}

\item{col_pts}{Colour for individual study points.  Default \code{"#2166AC"}.}

\item{col_curve}{Colour for the theoretical weight curve.  Default \code{"#D6604D"}.}

\item{xlab}{X-axis label.}

\item{ylab}{Y-axis label.}

\item{main}{Plot title.}

\item{show_labels}{Logical.  If \code{TRUE} (default), labels outlier points.}

\item{...}{Additional graphical arguments passed to \code{plot}.}
}
\value{
Invisibly returns a data frame with study-level weight information.
}
\description{
Plots DR-Meta study weights against design robustness (\eqn{\mathrm{DR}_i}),
illustrating the monotone ordering of Lemma 3 (Hait, 2025).  An overlaid
curve shows the theoretical weight function holding sampling variance at
its median value.
}
\examples{
set.seed(42)
k <- 12
dr <- runif(k, 0.1, 0.9)
vi <- runif(k, 0.01, 0.05)
yi <- rnorm(k, 0.3, sqrt(vi + 0.04 * exp(-2 * dr)))
fit <- drmeta(yi, vi, dr)
dr_plot(fit)

}
