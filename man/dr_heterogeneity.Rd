% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dr_heterogeneity.R
\name{dr_heterogeneity}
\alias{dr_heterogeneity}
\title{Heterogeneity Diagnostics for DR-Meta}
\usage{
dr_heterogeneity(object)
}
\arguments{
\item{object}{A fitted \code{"drmeta"} object from \code{\link{drmeta}}.}
}
\value{
A list with three elements:
\describe{
\item{\code{summary}}{A one-row data frame with: \code{k}, \code{Q}, \code{df}, \code{pval},
\code{tau2_mean} (mean design-specific heterogeneity), \code{I2}, \code{H2}.}
\item{\code{decomposition}}{A one-row data frame with the Proposition 6
decomposition: \code{tau2_design_explained}, \code{tau2_residual},
\code{tau2_total}, \code{R2_DR} (proportion explained by design).}
\item{\code{contributions}}{A data frame with per-study Q contributions:
\code{study}, \code{DR}, \code{tau2_i}, \code{q_i}, \code{pct_Q}.}
}
}
\description{
Computes a suite of heterogeneity statistics for a fitted \code{"drmeta"}
model, including Cochran's Q (with DR-Meta weights), \eqn{I^2}, \eqn{H^2},
the design-residual variance decomposition of Proposition 6 (Hait, 2025),
and per-study contributions to \eqn{Q}.
}
\section{Design-Residual Decomposition (Proposition 6)}{

The total between-study heterogeneity can be decomposed as:
\deqn{\tau^2_{\text{total}} = \mathbb{E}[\tau^2(\mathrm{DR}_i)]
  + \mathrm{Var}(u_i \mid \mathrm{DR}_i),}
where the first term is the \strong{design-explained} heterogeneity
(captured by the variance function) and the second is the
\strong{design-residual} heterogeneity (unexplained by DR).  This decomposition
is analogous to R-squared in meta-regression.

The design-explained proportion is
\deqn{R^2_{\mathrm{DR}} =
  \frac{\mathbb{E}[\tau^2(\mathrm{DR}_i)]}{\tau^2_{\mathrm{total}}}.}
}

\examples{
set.seed(42)
k <- 15
dr <- runif(k, 0.1, 0.9)
vi <- runif(k, 0.01, 0.05)
tau2_true <- 0.04 * exp(-2 * dr)
yi <- rnorm(k, 0.3, sqrt(vi + tau2_true))

fit <- drmeta(yi, vi, dr)
het <- dr_heterogeneity(fit)
het$summary
het$decomposition
het$contributions

}
\references{
Hait, S. (2025). \emph{Design-Robust Meta-Analysis: A Variance-Function
Framework for Causal Credibility}. Proposition 6.
}
