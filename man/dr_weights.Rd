% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dr_weights.R
\name{dr_weights}
\alias{dr_weights}
\title{Compute DR-Meta Study Weights}
\usage{
dr_weights(
  vi,
  dr,
  tau0sq,
  gamma,
  vfun = c("exponential", "linear"),
  normalise = c("none", "sum1", "pct")
)
}
\arguments{
\item{vi}{Numeric vector of sampling variances (\eqn{v_i > 0}).}

\item{dr}{Numeric vector of design robustness indices in \eqn{[0,1]}.}

\item{tau0sq}{Non-negative scalar: estimated baseline heterogeneity
\eqn{\hat\tau_0^2}.}

\item{gamma}{Non-negative scalar: estimated variance-function slope
\eqn{\hat\gamma}.}

\item{vfun}{Variance function: \code{"exponential"} (default) or \code{"linear"}.}

\item{normalise}{Character: \code{"none"} (raw weights, default), \code{"sum1"}
(divide by sum so weights sum to 1), or \code{"pct"} (multiply by 100 after
normalising to sum to 1).}
}
\value{
A numeric vector of weights, the same length as \code{vi}.
}
\description{
Returns the DR-Meta inverse-total-variance weights
\deqn{w_i = \frac{1}{v_i + \hat\tau^2(\mathrm{DR}_i)},}
given estimated variance-function parameters.  Optionally normalises weights
to sum to 1 or to 100.
}
\details{
This function is primarily a utility for diagnostics and visualisation;
weights are also returned as part of the \code{"drmeta"} object produced by
\code{\link{drmeta}}.
}
\examples{
vi  <- c(0.02, 0.03, 0.015, 0.025, 0.01)
dr  <- c(0.9,  0.4,  0.7,   0.2,   1.0)
dr_weights(vi, dr, tau0sq = 0.04, gamma = 1.5)
dr_weights(vi, dr, tau0sq = 0.04, gamma = 1.5, normalise = "pct")

}
\seealso{
\code{\link{drmeta}}, \code{\link{dr_variance}}
}
