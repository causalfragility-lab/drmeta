% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dr_plot.R
\name{dr_forest}
\alias{dr_forest}
\title{Forest Plot for DR-Meta}
\usage{
dr_forest(
  object,
  order_by = c("dr", "yi", "none"),
  xlab = "Effect size",
  main = "DR-Meta Forest Plot",
  col_point = "#2166AC",
  col_diamond = "#D6604D",
  col_dr = "#4DAC26",
  show_dr = TRUE,
  xlim = NULL,
  cex_study = 0.8,
  ...
)
}
\arguments{
\item{object}{A fitted \code{"drmeta"} object.}

\item{order_by}{Character: \code{"dr"} (default, sort by DR_i descending),
\code{"yi"} (sort by effect size), or \code{"none"} (original order).}

\item{xlab}{X-axis label.  Default \code{"Effect size"}.}

\item{main}{Plot title.}

\item{col_point}{Colour for study-level estimate points.  Default \code{"#2166AC"}.}

\item{col_diamond}{Colour for the summary diamond.  Default \code{"#D6604D"}.}

\item{col_dr}{Colour for the DR bar on the left.  Default \code{"#4DAC26"}.}

\item{show_dr}{Logical.  If \code{TRUE} (default), displays a coloured DR bar
indicating design robustness strength.}

\item{xlim}{Numeric vector of length 2 for x-axis limits.  If \code{NULL},
computed automatically.}

\item{cex_study}{Scaling factor for study labels.  Default 0.8.}

\item{...}{Further graphical arguments (ignored).}
}
\value{
Invisibly returns the data frame used for plotting (ordered studies).
}
\description{
Draws a forest plot for a fitted \code{"drmeta"} model using base graphics.
Studies are ordered by design robustness (largest DR at top by default).
Point sizes are proportional to DR-Meta weights; a vertical reference line
and summary diamond are included.
}
\examples{
set.seed(42)
k <- 10
dr <- runif(k, 0.1, 0.9)
vi <- runif(k, 0.01, 0.05)
yi <- rnorm(k, 0.3, sqrt(vi + 0.04 * exp(-2 * dr)))
fit <- drmeta(yi, vi, dr)
dr_forest(fit)

}
