% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dr_score.R
\name{dr_from_design}
\alias{dr_from_design}
\title{Design Robustness from Study Design Type}
\usage{
dr_from_design(
  design,
  custom_map = NULL,
  default_score = 0.25,
  warn_unknown = TRUE
)
}
\arguments{
\item{design}{Character vector of design type labels (case-insensitive).}

\item{custom_map}{Optional named numeric vector to override or add design
types, e.g. \code{c(my_design = 0.65)}.}

\item{default_score}{Numeric score assigned to unrecognised design labels.
Default is 0.25 (conservative).}

\item{warn_unknown}{Logical.  If \code{TRUE} (default), warns about unrecognised
labels.}
}
\value{
A numeric vector of design robustness scores in \eqn{[0,1]},
the same length as \code{design}.
}
\description{
Maps a vector of study design type labels to a numeric design robustness
score in \eqn{[0,1]}, using a pre-specified hierarchy of causal credibility.
This is a convenient starting point for operationalising the DR index when
only design type is available.
}
\details{
The default hierarchy follows the causal inference literature (Rubin, 2008;
Rosenbaum, 2010; Imbens & Rubin, 2015):\tabular{ll}{
   Design type label \tab Default DR score \cr
   \code{"rct"} \tab 1.00 \cr
   \code{"rd"}, \code{"rdd"} \tab 0.75 \cr
   \code{"iv"} \tab 0.75 \cr
   \code{"did"}, \code{"diff_in_diff"} \tab 0.60 \cr
   \code{"matching"}, \code{"psm"} \tab 0.50 \cr
   \code{"ipw"}, \code{"propensity"} \tab 0.45 \cr
   \code{"regression"}, \code{"ols"} \tab 0.25 \cr
   \code{"cross_section"} \tab 0.20 \cr
   \code{"descriptive"} \tab 0.10 \cr
}


Users can override or extend this table via the \code{custom_map} argument.
}
\examples{
designs <- c("RCT", "DiD", "OLS", "IV", "matching", "unknown_design")
dr_from_design(designs)

# Custom override
dr_from_design(designs, custom_map = c(unknown_design = 0.35))

}
\seealso{
\code{\link{dr_score}}, \code{\link{drmeta}}
}
