% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dr_weights.R
\name{dr_variance}
\alias{dr_variance}
\title{Evaluate the DR-Meta Variance Function}
\usage{
dr_variance(dr, tau0sq = NULL, gamma = NULL, vfun = c("exponential", "linear"))
}
\arguments{
\item{dr}{Numeric vector of design robustness values in \eqn{[0,1]}, or
a fitted \code{"drmeta"} object (in which case \code{tau0sq}, \code{gamma}, and
\code{vfun} are extracted automatically and \code{dr} from the model is used).}

\item{tau0sq}{Scalar \eqn{\hat\tau_0^2}.  Ignored if \code{dr} is a
\code{"drmeta"} object.}

\item{gamma}{Scalar \eqn{\hat\gamma}.  Ignored if \code{dr} is a
\code{"drmeta"} object.}

\item{vfun}{Variance function: \code{"exponential"} or \code{"linear"}.  Ignored if
\code{dr} is a \code{"drmeta"} object.}
}
\value{
A numeric vector of \eqn{\tau^2(\mathrm{DR}_i)} values.
}
\description{
Evaluates \eqn{\tau^2(\mathrm{DR};\,\psi)} for a grid of DR values or for
the study-level DR indices from a fitted \code{"drmeta"} model.
Useful for visualising how heterogeneity varies with design robustness.
}
\examples{
# Evaluate on a grid
grid <- seq(0, 1, by = 0.1)
tau2 <- dr_variance(grid, tau0sq = 0.04, gamma = 1.5)
plot(grid, tau2, type = "l", xlab = "DR", ylab = expression(tau^2))

# Extract from a fitted model
set.seed(1)
fit <- drmeta(yi = rnorm(10, 0.3), vi = runif(10, 0.01, 0.05),
              dr = runif(10))
dr_variance(fit)

}
\seealso{
\code{\link{drmeta}}, \code{\link{dr_weights}}
}
