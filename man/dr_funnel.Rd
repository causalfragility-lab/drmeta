% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dr_pub_bias.R
\name{dr_funnel}
\alias{dr_funnel}
\title{Funnel Plot for DR-Meta}
\usage{
dr_funnel(
  object,
  contours = TRUE,
  xlab = "Effect size",
  ylab = "Standard error",
  main = "DR-Meta Funnel Plot",
  col_low = "#D6604D",
  col_high = "#2166AC"
)
}
\arguments{
\item{object}{A fitted \code{"drmeta"} object.}

\item{contours}{Logical.  If \code{TRUE} (default), adds 95\\% and 99\\% funnel
contours around the pooled estimate.}

\item{xlab}{X-axis label.}

\item{ylab}{Y-axis label (default: reversed SE axis).}

\item{main}{Plot title.}

\item{col_low}{Colour for low-DR studies.  Default \code{"#D6604D"}.}

\item{col_high}{Colour for high-DR studies.  Default \code{"#2166AC"}.}
}
\value{
Invisibly returns \code{NULL}.
}
\description{
Creates a funnel plot for a fitted \code{"drmeta"} model.  The horizontal
axis shows effect-size estimates; the vertical axis shows standard errors.
Point sizes are proportional to DR-Meta weights and point colour encodes
design robustness.
}
\examples{
set.seed(42)
k <- 15
dr <- runif(k)
vi <- runif(k, 0.01, 0.06)
yi <- rnorm(k, 0.3, sqrt(vi + 0.04 * exp(-1.5 * dr)))
fit <- drmeta(yi, vi, dr)
dr_funnel(fit)

}
