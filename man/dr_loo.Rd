% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dr_loo.R
\name{dr_loo}
\alias{dr_loo}
\title{Leave-One-Out Influence Diagnostics for DR-Meta}
\usage{
dr_loo(object, parallel = FALSE, mc.cores = NULL)
}
\arguments{
\item{object}{A fitted \code{"drmeta"} object from \code{\link{drmeta}}.}

\item{parallel}{Logical.  If \code{TRUE} and the \pkg{parallel} package is
available, uses \code{parallel::mclapply} for the LOO loop (Unix/macOS
only).  Default \code{FALSE}.}

\item{mc.cores}{Integer.  Number of cores for parallel execution.  Default
is \code{parallel::detectCores() - 1}.}
}
\value{
A list with:
\describe{
\item{\code{summary}}{A data frame with one row per study and columns:
\code{study}, \code{DR}, \code{est_loo} (LOO pooled estimate), \code{ci.lb_loo},
\code{ci.ub_loo}, \code{tau0sq_loo}, \code{gamma_loo}, \code{delta_mu} (change in
estimate), \code{delta_tau0sq}, \code{delta_gamma}, \code{influential} (logical:
|delta_mu| > 2 * SE of full model).}
\item{\code{full}}{The original full-model \code{"drmeta"} object.}
}
}
\description{
For each study, re-fits the DR-Meta model after excluding that study and
records how the pooled estimate, confidence interval, variance-function
parameters, and heterogeneity change.  Studies with large absolute
\eqn{\Delta\hat\mu} or large shifts in \eqn{\hat\tau_0^2}/\eqn{\hat\gamma}
are considered influential.
}
\examples{
set.seed(7)
k  <- 12
dr <- runif(k, 0.1, 0.9)
vi <- runif(k, 0.01, 0.05)
tau2_true <- 0.04 * exp(-2 * dr)
yi <- rnorm(k, 0.3, sqrt(vi + tau2_true))

fit <- drmeta(yi, vi, dr)
loo <- dr_loo(fit)
loo$summary

}
