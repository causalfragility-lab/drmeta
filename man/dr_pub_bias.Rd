% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dr_pub_bias.R
\name{dr_pub_bias}
\alias{dr_pub_bias}
\title{Publication Bias Assessment for DR-Meta (PET/PEESE/Egger/Funnel)}
\usage{
dr_pub_bias(object, test = c("PET", "PEESE", "Egger"), alpha = 0.05)
}
\arguments{
\item{object}{A fitted \code{"drmeta"} object from \code{\link{drmeta}}.}

\item{test}{Character vector of tests to run; any subset of
\code{c("PET", "PEESE", "Egger")}.  Default: all three.}

\item{alpha}{Significance level for PET/PEESE intercept test.  Default 0.05.}
}
\value{
A list with elements:
\describe{
\item{\code{PET}}{Summary of PET regression (data frame with intercept,
slope, SE, z, p).}
\item{\code{PEESE}}{Summary of PEESE regression.}
\item{\code{Egger}}{Summary of Egger regression.}
\item{\code{recommendation}}{Character string: use PET intercept if PET slope
is significant and effect is small; use PEESE intercept otherwise.}
}
}
\description{
Performs a suite of publication-bias and small-study-effects tests adapted
for DR-Meta weights.  PET and PEESE regressions use
\eqn{w_i = 1/\hat\sigma_i^2 = 1/(v_i + \hat\tau^2(\mathrm{DR}_i))} as
regression weights, so precision is design-adjusted.
}
\details{
\strong{PET} (Precision Effect Test): regresses \eqn{y_i} on \eqn{se_i =
\sqrt{v_i}}, with DR-Meta weights.  A significant slope implies small-study
bias; the intercept estimates the publication-bias-corrected effect.

\strong{PEESE} (Precision Effect Estimate with Standard Error): regresses
\eqn{y_i} on \eqn{v_i}, with DR-Meta weights.  Generally preferred when the
true effect is non-zero.

\strong{Egger test}: Egger-type regression using the standardised effect
\eqn{y_i / se_i} on \eqn{1 / se_i}, with DR-Meta weights.

\strong{Funnel asymmetry}: classic funnel plot with DR-Meta summary.
}
\examples{
set.seed(99)
k  <- 20
dr <- runif(k, 0.1, 0.9)
vi <- runif(k, 0.005, 0.08)
# Introduce small-study effect: smaller studies overestimate
yi <- rnorm(k, 0.3 + 0.5 * sqrt(vi), sqrt(vi + 0.03 * exp(-1.5 * dr)))
fit <- drmeta(yi, vi, dr)
pb  <- dr_pub_bias(fit)
pb$PET
pb$PEESE
pb$recommendation

}
